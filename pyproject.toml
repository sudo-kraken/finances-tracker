[project]
name = "finances-tracker"
version = "1.2.5"
description = "Financial Tracker Application"
readme = "README.md"
keywords = ["python", "selfhosted", "homelab", "automation", "devops", "infrastructure", "it", "itops", "sysadmin", "systemadministration", "orchestration", "configurationmanagement"]
authors = [{ name = "Joe Harrison", email = "sudo-kraken@prxy.8shield.net" }]
license = { file = "LICENSE" }
requires-python = ">=3.10"
dependencies = [
    "authlib>=1.8.0,<1.9.0",
    "click>=8.3.3",
    "flask>=3.1.2",
    "flask-sqlalchemy>=3.1.0,<4.0.0",
    "flask-wtf>=1.1.2,<2.0.0",
    "flask-login>=0.6.3,<1.0.0",
    "werkzeug>=3.1.3,<3.2.0",
    "gunicorn>=23.0.0",
    "idna>=3.15",
    "python-dateutil>=2.8.2,<3.0.0",
    "requests>=2.33.0",
    "urllib3>=2.7.0",
]

[project.optional-dependencies]
dev = [
    "atheris>=3.1.0,<4.0.0; sys_platform == 'linux' and platform_machine == 'x86_64' and platform_python_implementation == 'CPython' and python_version == '3.13'",
    "ruff>=0.13.1",
    "pygments>=2.20.0",
    "pytest>=9.0.3",
    "pytest-cov>=6.0.0",
]

[tool.ruff]
line-length = 120
target-version = "py310"

[tool.ruff.lint]
select = [
    "E",   # pycodestyle errors
    "W",   # pycodestyle warnings
    "F",   # pyflakes
    "I",   # isort
    "N",   # pep8-naming
    "UP",  # pyupgrade
    "B",   # flake8-bugbear
    "C4",  # flake8-comprehensions
    "SIM", # flake8-simplify
]
ignore = [
    "E501", # line too long (handled by formatter)
]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
pythonpath = ["."]
addopts = [
    "--strict-markers",
    "--strict-config",
    "--cov=app",
    "--cov-report=term-missing",

]
filterwarnings = [
  "ignore:datetime\\.datetime\\.utcnow\\(\\) is deprecated:DeprecationWarning",
]

[tool.coverage.run]
source = ["app"]
omit = ["*/tests/*"]

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "def __repr__",
    "raise AssertionError",
    "raise NotImplementedError",
    "if __name__ == .__main__.:",
]
